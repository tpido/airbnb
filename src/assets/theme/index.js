export const theme = {
  color: {
    primaryColor: "#ff385c",
    secondaryColor: "#398288",
  },
  mixin: {
    hoverBoxShadow: `&:hover {
      box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.15);
      transition: box-shadow 200ms ease;
    }`,
  },
};
